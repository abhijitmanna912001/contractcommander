DATABASE_URL="postgresql://user:password@localhost:5432/contractcommander?schema=public"
PORT=4000

# Anthropic API key used by the commander/sub-agent/critic pipeline (see /server/src/agents).
ANTHROPIC_API_KEY=your_anthropic_api_key_here
# Optional: override the default model (claude-opus-5) used for all pipeline LLM calls.
ANTHROPIC_MODEL=claude-opus-5
